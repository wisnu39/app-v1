# docker/

Docker configuration — to be created in infrastructure setup task.

Planned:
- `api/`      — NestJS API Dockerfile
- `web/`      — React Frontend Dockerfile
- `postgres/` — PostgreSQL config
- `redis/`    — Redis config
- `nginx/`    — Nginx reverse proxy
- `docker-compose.yml`
- `docker-compose.dev.yml`
