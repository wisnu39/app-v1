# docker/nginx/

Nginx reverse proxy configuration — to be implemented in Phase 11 (Production Hardening).

Planned:
- `nginx.conf`         — main nginx config
- `conf.d/api.conf`    — upstream proxy to NestJS API
- `conf.d/web.conf`    — static frontend serving
- SSL/TLS termination config
