# scripts/

Utility scripts — to be created per phase.

Planned:
- `setup/`      — environment setup scripts
- `migration/`  — database migration helpers
- `seed/`       — database seeding scripts
- `backup/`     — backup utilities
- `deployment/` — deployment helpers
